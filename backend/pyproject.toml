[project]
name = "optus-hub-backend"
version = "0.1.0"
description = "Backend scaffold for Optus Hub"
requires-python = ">=3.9"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py39"
